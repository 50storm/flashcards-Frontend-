name: Deploy Nuxt 3 to Xserver via SFTP

on:
  push:
    branches:
      - main  # デプロイ対象ブランチ

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Build & Generate static site
        run: |
          npm run build
          npm run generate

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "${{ secrets.XSERVER_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          touch ~/.ssh/known_hosts
          ssh-keyscan -p 10022 -H ${{ secrets.XSERVER_HOST }} >> ~/.ssh/known_hosts || echo "keyscan skipped"

      - name: Deploy via SFTP
        env:
          HOST: ${{ secrets.XSERVER_HOST }}
          USER: ${{ secrets.XSERVER_USER }}
          PORT: ${{ secrets.XSERVER_PORT }}
          REMOTE_PATH: ${{ secrets.XSERVER_REMOTE_PATH }}
        run: |
          echo "🚀 Deploying to Xserver via SFTP..."
          sftp -i ~/.ssh/id_rsa -P ${PORT:-10022} -o StrictHostKeyChecking=no ${USER}@${HOST} <<'EOF'
          mkdir -p ${REMOTE_PATH}
          cd ${REMOTE_PATH}
          lcd ./.output/public
          put -r *
          bye
          EOF
          echo "✅ Deployment complete!"
